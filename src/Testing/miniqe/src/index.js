describe("Array", () => {
  describe("#push", () => {
    it("should add given element to the end of an array", () => {});

    it("should add more elements when several elements are passed as param", () => {});

    it("should return the new length of the array", () => {});
  });
});

mocha.run();
