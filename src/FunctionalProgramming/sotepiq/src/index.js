// Count elements that return true with `test`
function count(test, array) {
  // SOLUTION GOES HERE
}

// Get a function that tests equality to `x`
function equals(x) {
  // SOLUTION GOES HERE
}

// Get number of zeroes in `array`
function countZeroes(array) {
  // SOLUTION GOES HERE
}

console.log(countZeroes([1, 2, 0, 0, 4, 1, 0, 2, 0, 1]));

// OUTPUT: 4
